server:
  port: 8888
  
spring:
  application:
    name: config-server
  profiles:
    active:
    - true
    include:
    - github
  cloud:
    config:
      server:
        git:
          uri: https://github.com/iortizf/akkihazlo/tree/master/config-repository
          